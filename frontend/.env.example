# Backend API manzili. Bo'sh bo'lsa — nisbiy `/api/v1` ishlatiladi (nginx proxy).
VITE_API_BASE_URL=
# Dev rejimda vite proxy uchun backend manzili
VITE_DEV_API_PROXY=http://localhost:8000
# Telegramsiz brauzerda sinash uchun (backendda ALLOW_DEV_LOGIN=true bo'lishi kerak)
VITE_DEV_LOGIN_SECRET=dev-secret
VITE_APP_NAME=Pardachi
